# rspamd.conf.local

uprotector { 
    .include(try=true,priority=5) "${DBDIR}/dynamic/atchprotector.conf"
    .include(try=true,priority=1,duplicate=merge) "$LOCAL_CONFDIR/local.d/atchprotector.conf"
    .include(try=true,priority=10) "$LOCAL_CONFDIR/override.d/atchprotector.conf"
}
